import 'package:flutter/material.dart';

class SelfLoveChallenges extends StatelessWidget {
  const SelfLoveChallenges({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [Text("Self Love")],
      ),
    );
  }
}
